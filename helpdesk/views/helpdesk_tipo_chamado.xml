<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View -->
    <record id="helpdesk_tipo_chamado_view_form" model="ir.ui.view">
        <field name="name">Formulário de Departamento</field>
        <field name="model">helpdesk.tipo.chamado</field>
        <field name="arch" type="xml">
            <form edit="true" delete="true">
                <sheet> 
                    <group>
                        <group>
                            <field name="sequencial"/>
                            <field name="name"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree(list) View -->
    <record id="helpdesk_tipo_chamado_view_tree" model="ir.ui.view">
        <field name="name">Lista de Departamentos</field>
        <field name="model">helpdesk.tipo.chamado</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequencial"/>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Act Window -->
    <record id='helpdesk_tipo_chamado_action' model='ir.actions.act_window'>
        <field name="name">Tipos de Chamados</field>
        <field name="res_model">helpdesk.tipo.chamado</field>
    </record>

    <!-- Act Window View -->
    <record id="link_helpdesk_tipo_chamado_tree" model="ir.actions.act_window.view">
        <field name="sequence" eval="10"/>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="helpdesk_tipo_chamado_view_tree"/>
        <field name="act_window_id" ref="helpdesk_tipo_chamado_action"/>
    </record>

    <record id="link_helpdesk_tipo_chamado_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="20"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="helpdesk_tipo_chamado_view_form"/>
        <field name="act_window_id" ref="helpdesk_tipo_chamado_action"/>
    </record>

</odoo>

